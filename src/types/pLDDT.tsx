export type pLDDTNeurosnap = {
    plddt: number[];
    max_pae: number;
    pae: number[][];
    ptm: number;
};
export type pLDDTModel = {
    residueNumber: number[];
    confidenceScore: number[];
    confidenceCategory: string[];
};
